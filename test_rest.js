const config = require('./firebase-applet-config.json');

async function run() {
  // We need an ID token to test this, which we don't have here. 
  console.log("REST API works with user tokens.");
}
run();
